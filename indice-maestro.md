# Índice Maestro — SmartInversión Marketing

Documento permanente y acumulativo de la Metodología Oficial de Trabajo 4.0. Vive en la raíz del repositorio (`./indice-maestro.md`). No se reemplaza en cada rotación — se actualiza incrementalmente como parte del ritual de cierre (Sección 9 de la metodología). Migrado desde el project knowledge (Metodología 3.0) el 2026-08-05, primera sesión bajo 4.0 / Modo A.

Convención de estados: **RESUELTO** (ruta real verificada, con fecha) / **PENDIENTE (BLOQUEANTE)** (infra transversal, bloquea cualquier endpoint nuevo, se resuelve como Objetivo Cero) / **PENDIENTE (NO BLOQUEANTE)** (pieza de dominio que no afecta el objetivo actual).

---

## Bloque A — Transversal

| Pieza | Estado | Ruta / Referencia |
|---|---|---|
| Metodología de trabajo | RESUELTO (2026-08-05) | `./METODOLOGIA_OFICIAL_DE_TRABAJO_4_0.md` (raíz) |
| Índice Maestro | RESUELTO (2026-08-05) | `./indice-maestro.md` (este archivo) |
| Registro de Patrones | RESUELTO (2026-08-05) | `./registro-de-patrones.md` |
| Instantánea de código (Repomix) | RESUELTO (2026-08-05) | `./repomix-output.txt` (raíz) — regenerar en cada cierre de iteración/rebanada |
| Graphify (hook + respaldo manual) | RESUELTO (verificado 2026-08-07) | Hook `post-commit` + comando manual de respaldo (`graphify cluster-only C:\Users\Usuario\Desktop\smartinversion-marketing`) ambos con evidencia real. Última corrida manual (2026-08-07, tras merge `a2d2c98` de S5-004 iteración 4): 2560 nodos, 3471 aristas, 237 comunidades. |
| Graphify — cobertura .sql | PENDIENTE (NO BLOQUEANTE) | Evidencia real del commit `17e822b`: falta la dependencia `tree_sitter_sql` (`pip install "graphifyy[sql]"`), por lo que los archivos `.sql` (migraciones, tests pgTAP) no aportan nodos al grafo de código — el grafo hoy solo cubre TypeScript/JS. No bloquea ningún objetivo de S4-010; instalar la dependencia si el usuario quiere que Graphify también indexe SQL. |
| Contrato F4 (segmentos S4-001..S4-011) | RESUELTO (verificado 2026-08-05) | `docs/f4-production-qa-contract.md` Sección 21 |
| Matriz de control de acceso (roles x tabla) | RESUELTO (verificado históricamente) | `docs/access-control-matrix.md` Sección 11 ("Production and QA matrix") |
| Regla de ejecución git en este proyecto | RESUELTO | Nunca ejecutar git vía `device_bash` (deja `index.lock` huérfano) — entregar siempre el comando exacto al usuario. Ver Registro de Patrones. |
| Regla de Rutas de Lectura Obligatorias en el Testigo (Modo A) | RESUELTO (2026-08-06) | `./METODOLOGIA_OFICIAL_DE_TRABAJO_4_0.md` Secciones 2/8/9. Commit `57a4c43`, mergeado a `main` en PR separado del código (merge commit `d3e40fc`, 3/3 checks) — deliberadamente no mezclado con el PR #69 de S5-002 iteración 2c. |

## Bloque B — Por dominio (F4)

### Rutas API (`src/app/api/v1/...`) — S4-009, todas CERRADAS y en `main` (PR #60, merge commit `058f10b`, 2026-08-05, 3/3 checks)

| Dominio | Estado | Referencia |
|---|---|---|
| content_versions / approvals (8 endpoints) | RESUELTO | commits `b0ce42d`..`4660508`, 80/80 PASS |
| scenes (3 endpoints) | RESUELTO | commits `55928bb`, `03b5509`, `e162a2e` |
| generation_attempts (3 endpoints) | RESUELTO | commits `f7c0914`, `00a748d`, `13dfcff` |
| assets / asset-links (2 endpoints) | RESUELTO | commits `cb1d186`, `cd85ab3` |
| qa (7 piezas: checklists, checklist-items, activate, reviews x2, item-results, defects x2) | RESUELTO | commits `3af6629`..`00f7919` — S4-009 completo |

**Integración a `main`:** S4-009 mergeado 2026-08-05 vía PR #60 (`feat/production-qa-private-api-s4-009` → `main`, merge commit `058f10b`, merge con "Create a merge commit", 3/3 checks pasados). `main` real ya incluye S4-001..S4-009. Falta S4-010 (rama `feat/f4-010-cross-surface-authorization`, PR B pendiente — ver fila "9 RPCs Comando F4" más abajo y `estado_integracion_main_f4.md` en memoria persistente).

### Suite transversal pgTAP — S4-010 (`cross_surface_authorization_test_suite_s4_010.test.sql`, rama `feat/f4-010-cross-surface-authorization`)

| Rebanada | Estado | Referencia |
|---|---|---|
| 1. scenes | RESUELTO | commit `50a079d`, 22/22 PASS |
| 2. generation_attempts | RESUELTO | commit `e2bd3d8`, 43/43 PASS |
| 3. assets | RESUELTO | commit `feaaf9a`, 78/78 PASS + migración correctiva `20260819000000_assets_campaign_manager_rls_recursion_fix_s4_010.sql` |
| 4. asset_links | RESUELTO (2026-08-05) | 20/20 aserciones nuevas, suite completa `Files=36, Tests=1304, Result: PASS` (1284+20, exacto, sin regresiones). Sin migración correctiva — no se encontró ningún bug real en esta rebanada. Commit `17e822b`. |
| 5. qa_reviews | RESUELTO (2026-08-05) | 36/36 aserciones nuevas, suite completa `Files=36, Tests=1340, Result: PASS` (1304+36, exacto, sin regresiones). Bug real encontrado y corregido: `s4_008_is_content_version_asset_authored` era SECURITY INVOKER y necesitaba leer `content_versions` internamente — ni `editor` ni `director_ai_operator` tienen policy SELECT sobre esa tabla, RLS silenciaba el join a 0 filas → falso negativo para editor en la lectura "related" de `qa_reviews`. Fix: convertir a SECURITY DEFINER (mismo precedente que la rebanada 3). Migración correctiva: `supabase/migrations/20260820000000_content_version_asset_authored_security_definer_fix_s4_010.sql`. Commit `5b46919`. **Alerta abierta:** el mismo helper respalda "editor Related R" en `approvals` (rebanada 7) — el fix ya aplica globalmente (misma función), pero verificar con evidencia real cuando esa rebanada se construya, no asumir. |
| 6. qa_defects | RESUELTO (2026-08-05) | 29/29 aserciones nuevas, suite completa `Files=36, Tests=1369, Result: PASS` (1340+29, exacto, sin regresiones, primer intento). Sin migración correctiva. Confirma con evidencia real la lectura literal ya decidida en S4-009 ("solo approver resuelve"): `qa_defects_*_assigned_update` no exige que el resolutor SEA approver, solo que `resolved_by`/`resolved_role_id` identifiquen a un approver activo — un rol asignado (creative_owner/director_ai_operator/editor) puede completar la resolución de su propio defecto si atribuye correctamente a un approver real, pero no puede auto-atribuirse. Commit `ef3428b`. |
| 7. approvals | RESUELTO (2026-08-05) | 26/26 aserciones nuevas, suite completa `Files=36, Tests=1395, Result: PASS` (1369+26, exacto, sin regresiones, **segundo intento real** tras el fix de autoría). Primer intento real había dado `Failed: 11` (tests 172-179, 183-185) por un bug de AUTORÍA del test (no de producción): a la sección de insert-proofs le faltó reafirmar `set local role authenticated;` tras el `reset role;` del fixture, así que los intentos de rol denegado corrieron bajo el rol bypass de la conexión (RLS nunca se evaluó), el primero escribió la fila real y los siguientes chocaron contra `approvals_content_version_unique`. Fix: `set local role authenticated;` explícito al abrir insert-proofs. Sin migración correctiva — no se encontró ningún bug real en esta rebanada. **Alerta de la rebanada 5 CERRADA**: editor "Related R" vía `s4_008_is_content_version_asset_authored` (SECURITY DEFINER desde rebanada 5) confirmado con evidencia real — los tests 183-185 (que antes fallaban por la colisión de fila, no por el helper) ahora pasan, lo que solo es posible si el helper resuelve correctamente sobre la fila con el id esperado (`ed000000-...0001`). Pendiente: comitear (test file corregido, un solo archivo, sin migración nueva). |
| 9 RPCs Comando F4 | RESUELTO — cobertura de autorización confirmada con evidencia real (2026-08-05) | `cross_surface_authorization_test_suite_s4_010.test.sql` sigue fuera de alcance para RPCs por diseño (cubre exactamente la Sección 11 del access-control-matrix, solo tablas), pero la autorización de las 9 RPCs Comando F4 no vive en pgTAP sino en la capa de ruta privada (Patrón Comando: RPC `security definer`, `execute` restringido a `service_role`, gate de rol humano aplicado por `authorizePrivateRoute` antes de invocar `serviceClient.rpc(...)`). Confirmado leyendo el contenido real (no por nombre) de los 9 archivos `tests/api/*-authorization.test.ts` dedicados, uno por RPC, cada uno importando la ruta real que envuelve la RPC y mockeando `fakeServiceClient`/`fakeUserClient` con fixtures de `assignment(roleCode)`: `qa-checklist-activate-authorization.test.ts` (activate_qa_checklist), `content-version-submit-qa-authorization.test.ts` (submit_content_version_for_qa), `content-version-promote-to-approval-pending-authorization.test.ts` (promote_content_version_to_approval_pending), `content-version-approve-authorization.test.ts` (approve_content_version), `content-version-reject-approval-authorization.test.ts` (reject_content_version_approval), `content-version-reject-qa-authorization.test.ts` (reject_content_version_qa), `approval-invalidate-authorization.test.ts` (invalidate_approval), `content-version-archive-authorization.test.ts` (archive_content_version), `content-version-create-export-asset-authorization.test.ts` (create_export_asset). Las 9 quedan cubiertas por el gate `content_version.approve` (approver-only) salvo `submit_content_version_for_qa` (creative_owner, `content_version.write`) y `activate_qa_checklist` (reutiliza `qa_checklist.write`, gate propio de approver activo en la RPC). No se requiere archivo de test nuevo ni rebanada adicional. |

### Progreso global F4

F4 = 11 segmentos (S4-001..S4-011). **Cerrados: S4-001..S4-011 = 11/11 (2026-08-06) — Fase 4 completa.** Los 10 segmentos de implementación están integrados a `main` real (S4-009 vía PR #60/`058f10b`, S4-010 vía PR #61/`899563a`, ambos "Create a merge commit", 3/3 checks — ver `estado_integracion_main_f4.md` en memoria persistente para la cadena de entrega completa S4-001..S4-010). `cross_surface_authorization_test_suite_s4_010.test.sql` completa las 7/7 tablas de la Sección 11 con evidencia real; las 9 RPCs Comando F4 quedan fuera del alcance de este archivo (ver fila arriba), con una verificación de cobertura real pendiente y NO bloqueante para fases futuras.

**Gate G4 — RATIFICADO Y CERRADO (2026-08-06).** Recomendación de `docs/g4-gate-review.md` §11 ("ADVANCE CONDITIONALLY") ratificada explícitamente por el product owner (delegación de criterio al asistente, con instrucción permanente de priorizar lo mejor y más seguro para el proyecto). PR `docs/g4-ratification` mergeada a `main` con evidencia real (capturas de pantalla del usuario): merge commit `8e54292`, 3/3 checks requeridos en verde (CI / Migration and schema/RLS checks, CI / Quality and Cloudflare build, CI / Secret scanning), rama borrada. Ese mismo run de CI contra el commit de merge satisface también el punto pendiente de `docs/g4-gate-review.md` §12 ("CI fresco de todo el repo"). Fase 5 ("Distribución"/"Medición") queda autorizada para planificación e implementación exclusivamente sintética, bajo las condiciones de la Sección 8 de `docs/g4-gate-review.md` — producción sigue explícitamente NO autorizada.

### S4-011 — Cruce de las 12 condiciones de `docs/g3-gate-review.md` §8 contra evidencia real de S4-001..S4-010 (2026-08-05)

`docs/decision-register.md` confirmado sin ninguna entrada F4 (D-01..D-14 son todas de fases anteriores; D-14 es la ratificación HYP-/CNT- del propio Gate G3). Cruce condición por condición, leyendo el contenido real de cada migración/contrato citado (no por nombre de archivo):

| # | Condición G3 §8 | Estado en S4-011 | Evidencia real |
|---|---|---|---|
| 1 | `generate_claim_code()` permisos + test de inserción autenticada | **CERRADA** | Migración `20260801000001_generate_claim_code_authorization_s4_001.sql` otorga `EXECUTE` a `authenticated`; test `generate_claim_code_authorization_s4_001.test.sql` prueba exactamente los 4 puntos que pedía la condición (grant authenticated, grant service_role, revoke anon, no acceso directo a `claim_code_sequences`, inserción autorizada exitosa, bloqueo sin rol). |
| 2 | Definir `content_versions.status` y su ciclo de vida QA/aprobación | **CERRADA** | `docs/f4-production-qa-contract.md` §4-5 (S4-001) fija los 7 estados oficiales y las 9 transiciones permitidas. Migración S4-006 (`content_versions_status_allowed` CHECK + trigger `content_versions_validate_status_transition`) implementa el grafo de 9 aristas verbatim. S4-007 implementa los gates del lifecycle de `content_items`; S4-009 (`content_version_qa_entry_gate`, `content_version_qa_changes_required_gate`) cierra las dos aristas que S4-006/S4-007 habían dejado explícitamente sin gate ("Foundation, not yet connected"), mapeando cada una de las 10 condiciones de entrada del contrato §8 a una comprobación física real. |
| 3 | Criterios exactos de aceptación de content-version / si claims-evidence-assets version-specific son requeridos | **CERRADA** | Contrato §7-10 (S4-001) fija el binding exacto versión+master+checksum+claims+evidencia+derechos. La migración de entrada S4-009 mapea las 10 condiciones del §8 una por una a columnas/filas físicas reales (script/caption no-blank, scenes+acceptance_criteria, master_asset_id+checksum, rights_status, content_claims con claim aprobado/vigente, qa_checklist activo por content_type). |
| 4 | Resolver o revisar explícitamente los calificadores de access-control sin soporte; sin acceso amplio interino | **NO RESUELTA, pero NO VIOLADA** | Los calificadores nombrados en G3 §7.5 (financial_models/investment_theses, calificadores de campaign_manager, "Related"/"evidence-needs only" de oportunidades-campañas-contenido) pertenecen a F2/F3, fuera del dominio F4. Contrato §16 (S4-001) y la fila de S4-008 en el contrato §21 ("preserving fail-closed unsupported qualifiers") documentan explícitamente que F4 no amplía RLS para compensar calificadores no soportados. Ningún rol recibió acceso adicional no documentado durante F4 — la condición sigue abierta (no es responsabilidad de F4 cerrarla) pero su cláusula de "no acceso amplio interino" se cumplió. |
| 5-6 | Ciclo de vida financial-model/thesis y convención de moneda | **NO TOCADA, sigue ABIERTA** | `financial_models` (S2-004) e `investment_theses` (S2-005) son tablas F2, sin ninguna referencia en migraciones S4-xxx. F4 no dependió de ellas ni las modificó. Condición se traslada intacta a la fase que corresponda. |
| 7 | Aprobar D-08/MC-REG-001 antes de activar el piloto real | **NO TOCADA, sigue ABIERTA** | Decisión de negocio/producto (`docs/decision-register.md` D-08, estado "Provisional"). Ninguna migración o PR S4-xxx la referencia. |
| 8 | Resolver D-06 consentimiento y D-07 retención | **NO TOCADA, sigue ABIERTA** | Decisiones legales/producto (D-06/D-07, estado "Conditioned"). F4 usa exclusivamente datos sintéticos (contrato §1); ninguna migración S4-xxx las referencia. |
| 9 | Roles privilegiados nombrados, MFA y controles de sesión | **NO TOCADA, sigue ABIERTA** | `docs/authentication-session-policy.md` (S1-001, 2026-07-21) no define MFA ni asignaciones nombradas; ninguna migración S4-xxx la toca. G0-R05 sigue "Still open" sin cambios. |
| 10 | Triage de warnings de CI/dependencias (Node 20 deprecation en `supabase/setup-cli@v1`, 6 hallazgos npm audit high-severity, warning de Supabase Edge Runtime) | **NO TRIADA, sigue ABIERTA** | `.github/workflows/ci.yml` sigue usando `supabase/setup-cli@v1` sin cambios; no se encontró evidencia de ningún S4-xxx que corrija los hallazgos de `npm audit` ni el warning de Edge Runtime. A documentar como deuda técnica pendiente en el Gate G4, no bloqueante para F4 (mismo tratamiento que G3 le dio). |
| 11 | Fase 4 solo con datos sintéticos | **CUMPLIDA** | Contrato §1 (S4-001) reafirma datos sintéticos únicamente y prohíbe integraciones externas (Runway, Director IA, TikTok, Meta). Todos los fixtures pgTAP de S4-001..S4-010 usan identidades/UUIDs sintéticos (`*.example.test`). |
| 12 | Cada gate posterior re-chequea sus condiciones relevantes | **EN CURSO** | Este mismo cruce es la ejecución de la condición 12 para S4-011; queda formalizarlo como tabla "Gate G3 conditions" (análoga a la §6 "Gate G2 conditions" de `docs/g3-gate-review.md`) dentro de `docs/g4-gate-review.md`. |

**S4-011 — CERRADO (2026-08-06).** `docs/requirements-traceability-f4.md` y `docs/g4-gate-review.md` mergeados a `main` (PR #62/`86d5508`, luego PR #63/`ea319b8` recreando `docs/g4-review`). D-15/D-16 ya entrados en `docs/decision-register.md` (confirmado leyendo el archivo real en disco). Decisión de `g4-gate-review.md` §11 ratificada por el product owner el 2026-08-06 (PR `docs/g4-ratification`, merge commit `8e54292`, 3/3 checks) — ver nota de Gate G4 arriba. **F4 completo, Gate G4 cerrado.** Único punto no bloqueante que sigue abierto en todo el proyecto: el listado de archivos untracked en la raíz (ver Bloque C / memoria persistente `estado_integracion_main_f4.md`), pendiente de que el usuario decida trackear/ignorar/dejar como está.

## Bloque B2 — Por dominio (F5)

**F5 iniciada (2026-08-06).** Alcance mapeado cruzando `docs/core-schema.md` §6.5-6.7/§10.16-10.21/§11.9 contra las migraciones existentes: `publications`/`tracking_links` (§6.5) y `form_sessions`/`lead_attribution`/`lead_status_events`/`metric_definitions`/`metric_observations`/`campaign_reports` (§6.6-6.7) — nada construido. `restricted.leads`/`restricted.form_submissions`/`restricted.lead_consents`/`restricted.lead_deliveries` YA existen como tablas foundation desde S1-010 (sin RLS de aplicación ni rutas). `learning_records` (§6.7) queda fuera de F5 — es F6, ya cerrado aparte (ver Registro de Patrones). `docs/access-control-matrix.md` §§12-15 ya fija el modelo de autorización objetivo para todo el dominio F5, sin construir todavía.

**S5-001 — CERRADO (2026-08-06).** `docs/f5-distribution-measurement-contract.md` mergeado a `main` (PR `docs/f5-001-contract`, merge commit `f0a41b0`, 3/3 checks, evidencia real por capturas). Fija el límite de fase F4/F5/F6, el catálogo y grafo de transiciones de `publications` (el catálogo ya existía en §11.9, el grafo es nuevo), reafirma sin modificar el gate de elegibilidad de publicación ya fijado en `docs/f4-production-qa-contract.md` §14, contrato mínimo de `tracking_links`/`metric_definitions`/`metric_observations`, y ratifica formalmente `docs/preliminary-form-contract.md` (S0-015) y `docs/lead-delivery-contract.md` (S0-016) como el contrato de captura/entrega de F5 — ambos ya aprobados en `main` desde Sprint 0, nunca implementados, re-verificados contra el esquema actual sin encontrar contradicción. Añade dos restricciones propias de la era Gate G4 que S0-015/S0-016 no pudieron fijar: prospectos exclusivamente sintéticos (D-06/D-07/D-08 sin resolver) y adaptadores de entrega exclusivamente deshabilitados/sintéticos (condición 11 de `docs/g4-gate-review.md` §8). Segmentación S5-002..S5-010 en el contrato §11. Pendiente no bloqueante: entrada del límite de fase F4/F5/F6 en `docs/decision-register.md` como nueva decisión (mismo patrón que D-13/D-15/D-16) — todavía no hecha.

**S5-002 — EN CURSO. Iteración 1/N CERRADA e integrada a `main` con evidencia real (2026-08-06).** `supabase/migrations/20260821000000_publications_lifecycle_s5_002.sql` + `supabase/tests/database/publications_lifecycle_s5_002.test.sql` mergeados a `main` (PR `feat/f5-002-publications-lifecycle`, merge commit `8bcbd3b`, 3/3 checks requeridos en verde — CI/Migration and schema/RLS checks, CI/Quality and Cloudflare build, CI/Secret scanning —, evidencia real por capturas, rama borrada). Alcance de esta iteración: tabla física `publications` (columnas de `docs/core-schema.md` §10.16), CHECK `publications_status_allowed` (ocho estados oficiales, contrato §4.1) y trigger `publications_validate_status_transition_trigger` (grafo de 15 transiciones, contrato §4.2) — postura "Foundation, not yet connected" (RLS habilitado, sin grants a `authenticated`, solo `service_role`; RLS por rol es S5-006). Validado por el usuario con `npx supabase db reset && npx supabase test db`: `Files=37, Tests=1436, Result: PASS` (1395+41, exacto, sin regresiones). Pendiente (iteración 2 de este mismo segmento): el servicio de transición controlado (RPCs con registro auditable) y el gate de elegibilidad §4.3 (lee la vigencia de aprobación/checksum/claims/evidencia/derechos/defectos críticos del `content_version` fuente) — deliberadamente no construidos todavía, mismo patrón S4-006→S4-009 usado para `content_versions`.

**S5-002 — Iteración 2a CERRADA e integrada a `main` con evidencia real (2026-08-06).** PR #67 `feat/f5-002-eligibility-gate` → `main`, mergeado (merge commit `4df28e5`, 3/3 checks pasados, evidencia real por capturas, rama borrada). Contenido: `supabase/migrations/20260822000000_publications_eligibility_gate_s5_002.sql` (función `public.is_publication_eligible(uuid)`, `stable security definer set search_path=''`, reutiliza `is_approval_currently_valid()` de S4-006 para las 5 condiciones compartidas con el contrato F4, añade sin defecto crítico abierto en `qa_defects` y sin dependencia controladora bloqueada/pausada leída de `state_transition_subjects` — `content_item≠'blocked'`, `campaign≠'paused'`; fail-closed; aún NO conectada a ningún trigger/ruta, eso es iteración 2b) + `supabase/tests/database/publications_eligibility_gate_s5_002.test.sql` (12 aserciones pgTAP, PASS).

**Seis fallos reales consecutivos, seis causas reales distintas encontradas y corregidas antes del merge, cada una confirmada leyendo esquema/trigger/función real (nunca adivinada):**

| # | Commit | Causa real (confirmada leyendo esquema/trigger real) | Fix aplicado |
|---|---|---|---|
| 1 | `58c34cd` | `roles.code='approver'` insertado duplicado (la base real ya lo trae sembrado) | `select id from public.roles where code='approver'` en vez de `insert` |
| 2 | `87e4b56` | `content_items` insertados directo en `state_transition_subjects.current_state='preproduction'`, estado con gate propio (S3-003) que exige `hypothesis_id` + `campaign_evidence` aprobado, ninguno presente en el fixture | Cambiado a `'backlog'`, único estado de `content_item` sin gate (confirmado leyendo S3-003 y S4-007 completos) |
| 3 | `b3c66e3` | 4 `content_versions` para el mismo `content_item_id` sin `version_number` explícito → colisión en el default `1` contra `unique(content_item_id, version_number)` | `version_number` explícito 1-4 por fila |
| 4 | `bfdd719` | `private_storage_objects` con `state='approved'`, `object_key` arbitrario y sin `storage_object_id` violaba 3 CHECK distintas (`key_is_opaque`, `approved_state_complete`, `storage_link_complete`), todas leídas verbatim de `20260722044116_private_storage_authorization_s1_005.sql` | Fila real en `storage.objects`, `object_key='<id>/1'`, `state='available'` (confirmado válido para `is_approval_currently_valid()`, línea `v_storage.state not in ('available','approved')`), siguiendo el patrón ya probado de `assets_rights_checksums_private_storage_s4_004.test.sql` |
| 5 | `e3b1d9e` | `s4_006_validate_approval_entry`/`s4_005_validate_review_entry`/`s4_005_validate_defect` exigen `s4_005_has_active_human_role(profile, role)`, que hace `join` contra `public.role_assignments` — no un flag en `public.roles`. El perfil de la fixture nunca tenía esa fila | Segundo perfil "Role Admin" + `insert into public.role_assignments` otorgando `approver` al perfil de la fixture |
| 6 | (mergeado en `4df28e5`) | `s4_006_validate_approval_entry` exige `content_versions.status='approval_pending'` en el momento del INSERT en `approvals` — las 4 versiones de Cases C/D/E/F se insertaban directo con `status='approved'`, que el trigger nunca acepta (nada transiciona el status en un INSERT directo, solo `approve_content_version()`) | Reescrito el fixture para pasar por la ruta real `qa_pending`→`approval_pending`→`approved`: checklist compartido de 8 dimensiones + 1 escena/criterio por versión + 32 `qa_reviews` en batch + `qa_review_item_results` + `promote_content_version_to_approval_pending()` + `approve_content_version()`, siguiendo el precedente ya probado de `final_approvals_invalidation_qa_queue_export_s4_006.test.sql` |

**S5-002 — Iteración 2b CERRADA e integrada a `main` con evidencia real (2026-08-06).** PR `feat/f5-002-eligibility-wiring` → `main`, mergeado (merge commit `6e00557`, 3/3 checks requeridos en verde, rama borrada). Contenido: `supabase/migrations/20260823000000_publications_ready_scheduled_eligibility_wiring_s5_002.sql` — conecta `is_publication_eligible()` (iteración 2a) al trigger `publications_validate_status_transition_trigger` (iteración 1), exigido únicamente en la arista `ready → scheduled` (lectura literal del contrato §4.3: el texto solo vincula el gate a esa arista exacta). Decisión de diseño documentada en el propio header de la migración: el gate vive en el trigger, no en un RPC nuevo, porque `publications` (iteración 1) ya otorga a `service_role` un `UPDATE` directo sin gatear — no existe todavía el servicio de transición controlado, así que el trigger es el único punto que una escritura directa de `service_role` no puede saltarse (mismo razonamiento ya usado por `s4_006_validate_approval_entry` sobre `approvals`). `scheduled → published` y `paused → scheduled` quedan deliberadamente sin re-gatear, con test dedicado probando explícitamente que NO se re-gatean.

**Hallazgo real de la iteración 2b:** al validar la migración nueva contra la cadena completa de migraciones reales, `publications_lifecycle_s5_002.test.sql` (iteración 1, ya mergeado) rompió en su aserción `ready -> scheduled is permitted` — su fixture alcanzaba `content_versions.status = 'approved'` con un insert directo, sin fila real en `approvals`, la misma anomalía que `is_publication_eligible()` rechaza por diseño. Mismo patrón ya registrado en Registro de Patrones ("Foundation, not yet connected" → los tests estructurales viejos quedan obsoletos A PROPÓSITO cuando el gate se conecta). Se reescribió ese fixture para pasar por la ruta real `qa_pending → approval_pending → approved`, documentado en el propio header del archivo. Test nuevo dedicado: `publications_ready_scheduled_eligibility_wiring_s5_002.test.sql` (11 aserciones: eligible permitido, inelegible rechazado con el errcode/mensaje exactos, y los tres límites de alcance — `ready → draft`, `scheduled → published`, `paused → scheduled` — probados explícitamente como NO gateados).

Validado localmente (Postgres 16 + pgTAP, cadena completa de 45 migraciones reales aplicada desde cero + la migración nueva) antes de la entrega: `Files=3, Tests=64, PASS` sobre los tres archivos `publications_*`. Confirmado por el usuario contra el stack real: `npx supabase db reset && npx supabase test db` → `Files=39, Tests=1459, Result: PASS` (1448 + 11, exacto, sin regresiones).

Pendiente al cierre de 2b (resuelto en 2c, ver abajo): la cascada de invalidación reactiva del §4.3. Sigue pendiente el servicio de transición controlado (RPCs con registro auditable) — deliberadamente no construido todavía.

**S5-002 — Iteración 2c CERRADA e integrada a `main` con evidencia real (2026-08-07).** PR #69 `feat/f5-002-invalidation-cascade` → `main`, mergeado (merge commit `7af6c4d`, 3/3 checks requeridos en verde, rama borrada). Contenido: `supabase/migrations/20260824000000_publications_invalidation_cascade_s5_002.sql` — trigger `AFTER INSERT` sobre `approval_invalidations` que, al invalidarse la aprobación de un `content_version`, transiciona toda `publications` dependiente que siga `scheduled → paused` o `published → withdrawn` (aristas ya existentes del grafo de iteración 1, sin arista nueva), con `record_business_audit_event()` propio por cada publicación afectada, atribuido al mismo actor/rol/correlation_id de la invalidación de origen + `supabase/tests/database/publications_invalidation_cascade_s5_002.test.sql` (9 aserciones pgTAP, PASS). Decisiones de diseño documentadas en el header de la migración: (1) el gate/cascada vive en el trigger sobre la tabla que registra el evento real (`approval_invalidations`), nunca dentro de la RPC `invalidate_approval()` que lo dispara — mismo razonamiento que iteración 2b y `s4_006_validate_approval_entry`; (2) mapeo `scheduled → paused` / `published → withdrawn` fijado explícitamente (el contrato §4.3 solo dice "toward paused or withdrawn" sin fijar el mapeo exacto); (3) alcance deliberadamente acotado a la invalidación de aprobación únicamente — defecto crítico nuevo y `content_item`/campaña bloqueado/pausado quedan para una iteración posterior.

Validado localmente (Postgres 16 + pgTAP, cadena completa de 46 migraciones reales aplicada desde cero): `Files=4, Tests=73, PASS`. Confirmado por el usuario contra el stack real: `npx supabase db reset && npx supabase test db` → `Files=40, Tests=1468, Result: PASS` (1459+9, exacto, sin regresiones).

**Hallazgo real de esta iteración, sin relación con el código:** el primer intento de merge quedó bloqueado por un incidente externo de GitHub Actions ("Major Outage" activo, confirmado dos veces vía https://www.githubstatus.com el 2026-08-06 a las 17:02 y 19:43 UTC), que canceló el check "CI / Secret scanning" dos veces seguidas antes de que gitleaks llegara a correr — nunca un hallazgo real. `.github/workflows/ci.yml` y `.gitleaks.toml` revisados completos sin nada anómalo. Por Regla 16 (evidencia contradice la hipótesis dos veces seguidas → pausar, no parchar) se detuvo el ciclo de reintentos hasta que el incidente se resolvió solo; el rerun del 2026-08-07 (run `31136399470`) pasó los 3 checks limpio sin ningún cambio de código.

Pendiente (no bloqueante, mismo segmento S5-002, sin fecha asignada): profundizar S5-002 con el defecto crítico nuevo / campaña bloqueada-pausada tras el scheduling, y el servicio de transición controlado con RPCs (registro auditable) — deliberadamente no construidos todavía. El 2026-08-07 se decidió explícitamente pasar a S5-003 primero (ninguna bloquea a la otra).

**S5-003 — Iteración 1/N CERRADA e integrada a `main` con evidencia real (2026-08-07).** PR `feat/f5-003-tracking-links-foundation` → `main`, mergeado (merge commit `9ea686e`, 3/3 checks requeridos en verde — CI/Migration and schema/RLS checks, CI/Quality and Cloudflare build, CI/Secret scanning —, rama borrada). Contenido: `supabase/migrations/20260825000000_tracking_links_foundation_s5_003.sql` — tabla física `tracking_links` (`docs/core-schema.md` §6.5; contrato §5: FKs a `campaigns`/`publications`, `variant` normalizado, `token` opaco, vocabulario mínimo `active`/`superseded`) + `public.generate_tracking_token()` (token opaco de 160 bits vía `encode(extensions.gen_random_bytes(20), 'hex')`, nueva extensión `pgcrypto` para el repo — antes solo `btree_gist`, S1-002 — sin relación con ningún id interno, satisface por construcción la prohibición del contrato §5 de codificar PII/secretos/id interno en forma reversible; a diferencia de `generate_claim_code()` (S2-006), que es deliberadamente de baja entropía/legible y necesita secuencia, un token de 160 bits al azar no necesita loop de reintento) + `supabase/tests/database/tracking_links_foundation_s5_003.test.sql` (18 aserciones pgTAP, PASS). Postura "Foundation, not yet connected" (mismo patrón S5-002 iteración 1): RLS habilitado, solo `service_role`, RLS por rol es S5-006.

Validado por el usuario contra el stack real: `npx supabase db reset && npx supabase test db` → `Files=41, Tests=1486, Result: PASS` (1468+18, exacto, sin regresiones).

Deliberadamente NO construido en esta iteración (queda para la iteración 2 de este mismo segmento, documentado en el header de la migración): la regla de que un token vale solo mientras la `publications` madre no esté `archived`/`withdrawn`, y la regla append-preserving de que una variante corregida crea un token nuevo en vez de mutar el vigente — el contrato §5 fija ambos invariantes pero deja el mecanismo exacto como decisión de implementación de este segmento, la misma clase de decisión que S5-002 resolvió con evidencia real en las iteraciones 2a/2b/2c, no un primer intento adivinado.

**Hallazgo operativo de esta iteración:** correr `git status`/`git log`/`git fetch` vía la herramienta de shell del asistente sobre este mismo repositorio dejó un `.git/index.lock` huérfano que bloqueó los comandos git reales del usuario en PowerShell poco después. Confirma con evidencia real la Regla de ejecución git ya fijada en Bloque A ("Nunca ejecutar git vía `device_bash`") — se extiende explícitamente a cualquier herramienta de shell del asistente que opere sobre la carpeta real del repo, no solo a "device_bash" por nombre.

**S5-003 — Iteración 2/N CERRADA e integrada a `main` con evidencia real (2026-08-07).** PR `feat/f5-003-tracking-links-validity-supersede` → `main`, mergeado (merge commit `75a4cc8`, 3/3 checks requeridos en verde, rama borrada). Contenido: `supabase/migrations/20260826000000_tracking_links_validity_and_supersede_s5_003.sql` — `tracking_links_supersede_prior_active_trigger` (`AFTER INSERT`, retira a `superseded` cualquier fila `active` previa que comparta `campaign_id`+`publication_id`+`variant`, sin mutar nunca el token retirado — regla append-preserving del contrato §5) + `public.is_tracking_link_valid(uuid)` (predicado `stable`, `status='active'` Y `publications.status not in ('archived','withdrawn')`, fail-closed, aún sin conectar a ninguna ruta — no existe superficie de captura/redención todavía, eso es S5-004+) + índice parcial `tracking_links_active_variant_idx` + `supabase/tests/database/tracking_links_validity_and_supersede_s5_003.test.sql` (25 aserciones pgTAP, PASS). Decisión de diseño documentada en el header: el trigger de supersede corre incondicionalmente (no verifica primero el estado de la publicación) — la frase del contrato "already in use by a live publication" explica la motivación de negocio, no una condición de activación del trigger.

Validado por el usuario contra el stack real: `npx supabase db reset && npx supabase test db` → `Files=42, Tests=1511, Result: PASS` (1486+25, exacto, sin regresiones).

**S5-003 — CERRADO (2026-08-07).** Ambas reglas del contrato §5 implementadas (iteración 1: fundación física; iteración 2: validez + supersede). Sin servicio de transición controlado (RPCs) para `tracking_links` — no existe ninguna arista de estado que lo requiera todavía (el `status` `active`/`superseded` se gestiona íntegramente por el trigger de supersede, no por escritura directa de un actor). Pendiente no bloqueante, mismo segmento: ninguna — S5-003 no tiene iteración 3 planeada; el próximo trabajo de F5 es otro segmento (S5-004 en adelante, sin decidir todavía).

**S5-004 — Iteración 1/N CERRADA e integrada a `main` con evidencia real (2026-08-07).** PR `feat/f5-004-form-sessions-foundation` → `main`, mergeado (merge commit `f5661e6`, 3/3 checks requeridos en verde, rama borrada). Contenido: `supabase/migrations/20260827000000_form_sessions_foundation_s5_004.sql` — tabla física `public.form_sessions` (columnas derivadas de `docs/preliminary-form-contract.md` §16.2/§17.1, únicas fuentes normativas ya que `docs/core-schema.md` §10 nunca definió esta tabla: `campaign_id → campaigns` resuelto, `tracking_link_id → tracking_links` nullable — resuelve el `tracking_token` opaco de S0-015 contra S5-003 en vez de guardar texto crudo —, las 7 propiedades de atribución §17.1 como texto normalizado nullable, `form_version`/`consent_notice_version` not null, `expires_at` not null sin TTL fijado — §33 del contrato deja el lifetime exacto como decisión abierta "before endpoint implementation") + cierra el FK pendiente que S1-010 había dejado documentado explícitamente en su propio header: `restricted.form_submissions.form_session_id → public.form_sessions(id)` + `supabase/tests/database/form_sessions_foundation_s5_004.test.sql` (16 aserciones pgTAP, PASS). Postura "Foundation, not yet connected": RLS habilitado, solo `service_role`.

Validado por el usuario contra el stack real: `npx supabase db reset && npx supabase test db` → `Files=43, Tests=1527, Result: PASS` (1511+16, exacto, sin regresiones).

Deliberadamente fuera de alcance de esta iteración (documentado en el header de la migración): las 4 rutas públicas de `docs/preliminary-form-contract.md` §14 (`GET /campaigns/{slug}`, `POST /form-sessions`, `POST /submissions`, `POST /events`) y cualquier RPC de creación/consumo de sesión — esta migración solo fija la forma de la fila que una RPC futura insertará, no construye esa RPC. `lead_attribution`/`lead_status_events` siguen sin columnas definidas en ningún documento aprobado (contrato F5 §6 lo dice explícitamente). S0-016 (entrega) completo, y anti-abuse/rate-limiting (capa de ruta, no de esquema) también fuera de alcance.

Pendiente (no bloqueante, mismo segmento S5-004): decidir la siguiente iteración — probablemente las rutas públicas mismas, un trabajo de capa de aplicación materialmente más grande que cualquier iteración de F5 hasta ahora, dado el alcance de validación/anti-abuse/idempotencia/PII que exige S0-015.

Pendiente: PR de cierre documental separada (este archivo + Registro de Patrones + regenerar `repomix-output.txt`), mismo patrón usado para S5-002 iteración 2c.

**S5-004 — Iteración 2/N CERRADA e integrada a `main` con evidencia real (2026-08-07).** PR #78 `feat/f5-004-campaigns-public-slug` → `main`, mergeado vía `gh pr merge --merge --delete-branch` (merge commit `5434509`, 3/3 checks requeridos en verde, rama borrada local y remota). Contenido: `supabase/migrations/20260828000000_campaigns_public_slug_s5_004.sql` — `public.campaigns.slug` (nullable, único, formato `^[a-z0-9]+(-[a-z0-9]+)*$`, 3-80 caracteres), primera pieza física de las 4 rutas públicas de `docs/preliminary-form-contract.md` §14. Distinto de `campaigns.code` (S1-008, formato fijo `CAM-<año>-<secuencia>`, no puede representar un slug de marketing como `mc-reg-001`). Asignación manual, sin generador automático. Sin cambios de RLS/grant — `campaigns` ya otorga `update` a `authenticated` desde S3-007/S4-008. Sin columna "es pública" nueva: `slug is not null` + `state_transition_subjects.current_state = 'active'` ya expresa lo que el contrato §15 llama "an active and public campaign" + `supabase/tests/database/campaigns_public_slug_s5_004.test.sql` (11 aserciones pgTAP, PASS). Validado por el usuario: `npx supabase db reset && npx supabase test db` → `Files=44, Tests=1538, Result: PASS` (1527+11, exacto, sin regresiones).

Deliberadamente fuera de alcance de esta iteración: la ruta `GET /api/v1/public/campaigns/{slug}` en sí, cualquier RPC de asignación de slug, y los catálogos de la respuesta (income_ranges/income_modes/consent notice) — ninguno de los tres aparece en el inventario de entidades de `docs/core-schema.md`; tratados como config de aplicación versionada, no tablas nuevas, salvo decisión en contrario.

Pendiente (no bloqueante, mismo segmento S5-004): la ruta pública misma y las 3 rutas restantes de §14, más el PR de cierre documental de esta iteración (este archivo + Registro de Patrones + `npx repomix`).

**S5-004 — Iteración 3/N CERRADA e integrada a `main` con evidencia real (2026-08-07).** PR `feat/f5-004-public-campaign-config-route` → `main`, mergeado (merge commit `95ebcbd`, 3/3 checks requeridos en verde, rama borrada). Contenido: `src/app/api/v1/public/campaigns/[slug]/route.ts` — primera de las 4 rutas públicas de `docs/preliminary-form-contract.md` §14, la primera ruta `/api/v1` del proyecto sin actor autenticado. Confirmado leyendo `src/lib/api/private-route.ts` antes de codear: `authorizePrivateRoute` siempre exige sesión Supabase (401 sin ella), así que no aplica aquí — la ruta lee exclusivamente vía el cliente `service_role` (`campaigns`/`state_transition_subjects` no otorgan nada a `anon`/`authenticated` para este propósito; esta ruta ES el límite de autorización, §6). Decisión de catálogos confirmada con el product owner antes de codear (pregunta explícita, no asumida): `income_ranges`/`income_modes`/consent notice viven como config de aplicación versionada en `src/lib/api/public-form-config.ts` (nuevo), no tablas — transcritos verbatim de §10/§11/§15. `not_found` (404) uniforme para slug malformado/inexistente/campaña no-`active`, nunca distinguido, para no permitir enumeración. Reutiliza el envelope de error S2-009 existente (`{error, correlation_id}`) en vez del ejemplo anidado de §22 (documentado en el header de la ruta para que una sesión futura no lo "corrija" sin cambiar las demás rutas). `tests/api/public-campaign-config-route.test.ts` (6 tests, primer archivo de test del proyecto sin harness `fakeUserClient`/`role_assignments`, ver Registro de Patrones). Validado por el usuario: `npx vitest run` del archivo nuevo (6/6 PASS) + suite completa `npm test` → `Files=46, Tests=338, Result: PASS` (332+6, exacto, sin regresiones) + `tsc --noEmit` limpio.

PR de cierre documental de esta iteración (este archivo + Registro de Patrones + `npx repomix`) mergeada a `main` (PR `docs/s5-004-iteracion-3-cierre`, merge commit `c1ffeef`, 3/3 checks requeridos en verde, rama borrada, 2026-08-07). `repomix-output.txt` regenerado y vigente a partir de ese commit.

**S5-004 — Iteración 4/N CERRADA (código) e integrada a `main` con evidencia real (2026-08-07).** PR `feat/f5-004-public-form-sessions-route` → `main`, mergeado (merge commit `a2d2c98`, 3/3 checks requeridos en verde, rama borrada). Graphify manual (`graphify cluster-only`) corrido con evidencia real tras el merge: 2560 nodos, 3471 aristas, 237 comunidades (bajaron de 240 a 237 tras el re-clustering; backup del grafo curado guardado en `2026-08-07/`), `GRAPH_REPORT.md`/`graph.json`/`graph.html` actualizados. Falta solo la PR de cierre documental (este archivo + Registro de Patrones + `npx repomix`, ambos ya actualizados en disco en esta sesión, pendientes de commit). `src/app/api/v1/public/form-sessions/route.ts` (nuevo) — segunda de las 4 rutas públicas de §14. Antes de codear se cruzó el blocking point explícito del contrato §33 ("Exact production form-session lifetime | Before endpoint implementation", alcanzado justo al construir este endpoint) — TTL de 60 minutos confirmado con el product owner vía pregunta explícita, no asumido. Sin RPC/migración nueva: insert plano vía `service_role` (`form_sessions` ya otorga `insert` a ese rol desde S5-004 iteración 1); no hay atomicidad multi-tabla que justifique una función `security definer` como `create_campaign`. `tracking_token` se resuelve contra `tracking_links` + `is_tracking_link_valid()` (S5-003) y nunca se guarda crudo; si no matchea la campaña de la sesión o no es válido, `tracking_link_id` queda `null` sin rechazar la sesión (Sección 17.2: atribución no verificable permanece `null`). Los 5 campos de `attribution` que no calzan con el formato normalizado de `form_sessions` se descartan a `null` en vez de rechazar la petición completa (Sección 16.2 ofrece "ignore" como comportamiento válido). Refactor: la lógica "slug → campaña pública activa" se extrajo de `GET /campaigns/{slug}` (iteración 3) a `src/lib/api/public-campaign.ts` (nuevo), reutilizada por ambas rutas para que la regla "pública y activa" no pueda divergir entre ellas — comportamiento idéntico confirmado (suite de tests de la iteración 3 sigue en verde sin cambios). `tests/api/public-form-sessions-route.test.ts` (17 tests nuevos). Validado por el usuario: `npm test` → `Files=47, Tests=355, Result: PASS` (338+17, exacto, sin regresiones) + `tsc --noEmit` limpio. Falta: commit/PR/merge (código) y PR de cierre documental separada.

**S5-004 — Iteración 5/N CERRADA (código) e integrada a `main` con evidencia real (2026-08-07).** PR `feat/f5-004-public-submissions-route` → `main`, mergeado (merge commit `fef2f18`, 3/3 checks requeridos en verde, rama borrada). `POST /api/v1/public/submissions` — tercera de las 4 rutas públicas de §14, la de mayor superficie de F5. Los 2 blocking points del contrato §33 se cruzaron con el product owner antes de codear: (1) normalización de teléfono — la primera decisión fue `libphonenumber-js`, pero el sandbox de esta sesión no tiene acceso al registro de npm (403 en `registry.npmjs.org` incluso para paquetes triviales), así que no se pudo instalar ni verificar la dependencia; el product owner confirmó entonces caer a un normalizador propio (`src/lib/api/public-submission-normalize.ts`) — reconoce exactamente una forma inequívoca (móvil chileno de 9 dígitos empezando en `9`, o ya con `+`/`56` explícito), nunca adivina código de país en casos ambiguos; (2) mecanismo de bypass sintético — header dedicado `X-Synthetic-Test-Key` validado contra el secreto de entorno `SYNTHETIC_TEST_SECRET` (mismo patrón de `resolveServerSecret` que `JOBS_SECRET`), pero a diferencia de ese endpoint admin-only un secreto ausente/incorrecto nunca rechaza la petición (evita filtrar que el header importa) — solo se loguea. Arquitectura: se requiere una RPC nueva `public.create_submission` (`security definer`, sin chequeo de actor) porque `restricted` no está en `supabase/config.toml`'s `[api] schemas` (`["public", "graphql_public"]`) — PostgREST rechaza cualquier `.from()`/`.schema("restricted")` sin importar los grants ya otorgados a `service_role` desde S1-010; DEFINER (no INVOKER) específicamente porque `restricted.form_submissions` otorga `insert/update/delete` a `service_role` pero deliberadamente NO `select` (S1-010), y el chequeo de replay-vs-conflict de idempotencia necesita un SELECT contra esa tabla. Migración: `supabase/migrations/20260829000000_public_submissions_atomic_write_s5_004.sql` — agrega `restricted.form_submissions.payload_hash` (nullable, para no romper el fixture pgTAP de la iteración 1 que inserta sin esa columna) y corrige `restricted.leads.intent_declared` de `text` a `boolean` (nunca tuvo una fila real). `is_test` queda forzado a `true` siempre, nunca lo pone la función en `false` — D-06/D-07 (`docs/decision-register.md` §8-9) siguen "Conditioned", no autorizan captura real todavía. Clasificación (§12): solo `prefiltered`/`early` se persisten (§12.3 permite rechazar `incomplete` antes de persistir, y este endpoint ya rechaza cualquier campo faltante/mal formado en la capa de ruta antes de llamar la RPC); duplicado (§12.4) se resuelve reutilizando el `lead_id` existente por email o teléfono normalizado, nunca como quinto valor de clasificación. Idempotencia (§20): la RPC reserva el slot con `insert ... on conflict (idempotency_key) do nothing` ANTES de tocar `leads`/`lead_consents`, así una petición concurrente perdedora nunca crea filas huérfanas. Hashes (payload de idempotencia + texto de aviso de consentimiento) se calculan en TypeScript vía Web Crypto (`sha256Hex`), no dentro de la función SQL — una sola implementación de hashing en el proyecto. `tests/api/public-submissions-route.test.ts` (32 tests nuevos) y `supabase/tests/database/public_submissions_atomic_write_s5_004.test.sql` (23 aserciones pgTAP nuevas). Validado por el usuario con evidencia real: `npm test` → `Files=48, Tests=387, Result: PASS` (355+32, exacto, sin regresiones); `npx supabase db reset && npx supabase test db` → `Files=45, Tests=1561, Result: PASS` (1538+23, exacto, sin regresiones); `tsc --noEmit` y `eslint .` limpios (verificados en el sandbox del asistente, cero warnings).

PR de cierre documental de esta iteración (este archivo + Registro de Patrones + `npx repomix`) mergeada a `main` (PR `docs/s5-004-iteracion-5-cierre`, merge commit `0e30dbd`, 3/3 checks requeridos en verde, rama borrada, 2026-08-07).

**S5-004 — Iteración 6/N CERRADA (código) e integrada a `main` con evidencia real (2026-08-07).** PR `feat/f5-004-public-events-route` → `main`, mergeado (merge commit `57f3186`, 3/3 checks requeridos en verde, rama borrada). `POST /api/v1/public/events` — cuarta y última ruta pública de §14. Decisión confirmada con el product owner antes de codear (pregunta explícita, no un blocking point del §33 sino una laguna real: ninguna tabla de eventos aparece en `docs/core-schema.md` §6.1-6.8, a diferencia de `form_sessions`): los eventos se registran como **logs estructurados** (`logInfo`), nunca una tabla nueva — `docs/minimum-observability.md` §6 clasifica explícitamente "Logs" como el mecanismo para "Domain and integration event catalog" (vs. "Audit", que sí persiste en `audit_events`). Consecuencia: esta ruta no toca la base de datos en absoluto — sin cliente `service_role`, sin caso 503, sin migración esta iteración. Del catálogo de 6 eventos de la Sección 25.1, solo se aceptan los 3 cuya columna "Authority" es "Client or server"/"Server preferred" (`form_started`, `form_validation_failed`, `form_submission_attempted`); los 3 restantes se rechazan con `422 catalog_value_invalid` porque su autoridad documentada es "Server" (`form_submission_received`/`form_submission_rejected` — dejar que un cliente anónimo los asevere contaminaría métricas de conversión) o "Derived" (`form_abandoned` — se calcula, nunca se envía). `field_name`/`validation_code` se validan contra el mismo vocabulario exacto que ya usa `POST /submissions` en sus propios `details.field`/`details.reason`, para que el vocabulario de analítica nunca diverja del real. Mismo patrón ya establecido de "campo de mejor esfuerzo -> degrada a `null`, nunca rechaza la petición completa" aplicado a `form_version`/`field_name`/`validation_code`/`client_timestamp`. El `form_session_id` crudo deliberadamente NO se loguea (seguí el mismo precedente que las otras 2 rutas de esta iteración, ninguna loguea el id de sesión). `tests/api/public-events-route.test.ts` (18 tests nuevos). Validado por el usuario: `npm test` → `Files=49, Tests=405, Result: PASS` (387+18, exacto, sin regresiones) + `tsc --noEmit`/`eslint .` limpios (verificados en el sandbox del asistente, cero warnings).

Con esto, las 4 rutas públicas de §14 quedan completas (iteraciones 3-6). PR de cierre documental de esta iteración (este archivo + Registro de Patrones + `repomix-output.txt`) mergeada a `main` (PR #87 `docs/s5-004-iteracion-6-cierre`, 3/3 checks, rama borrada, 2026-08-07).

**S5-004 — CERRADO como segmento (2026-08-07).** Decisión delegada al asistente por el product owner ("qué es lo más recomendable, confío en tu criterio"), resuelta cruzando el propio texto normativo: `docs/f5-distribution-measurement-contract.md` §11 fija la responsabilidad exacta de `S5-004` como "Implement the public capture surface (`form_sessions`, wiring of `restricted.form_submissions`/`leads`/`lead_consents`) per S0-015, synthetic-only" — ninguna mención a rate limiting/honeypot/challenge. Las 4 rutas de §14 cubren exactamente ese alcance con evidencia real (iteraciones 1-6). Los dos pendientes discutidos no son responsabilidad de S5-004 por el propio contrato:
- Rate limiting/challenge — §33 los difiere explícitamente a "before public deployment", no a ningún segmento S5-0xx nombrado.
- Honeypot/minimum-completion-time/origin allowlisting — documentados en el header de `src/app/api/v1/public/submissions/route.ts` como refuerzos de hardening, tampoco parte del alcance §11 de ningún segmento.

Ambos quedan como trabajo de hardening pre-despliegue-público, no bloqueante para el resto de F5 (S5-001..S5-010 nunca dependen de ellos) ni para Gate G5 — mismo tratamiento que otras piezas "NO TOCADA, sigue ABIERTA" de S4-011. Revisar explícitamente antes de cualquier autorización real de producción (condición ya fijada por Gate G4 §8, D-06/D-07/D-08 sin resolver). Próximo segmento: `S5-005` (entrega de leads — `restricted.lead_deliveries`, `outbox_events`, `lead_attribution`, adaptadores deshabilitados/sintéticos, per S0-016).

**S5-005 — Iteración 1/N CERRADA e integrada a `main` con evidencia real (2026-08-07).** PR #89 `feat/f5-005-lead-delivery-outbox-foundation` → `main`, mergeado (merge commit `dd7065b`, 3/3 checks requeridos en verde, rama borrada). Contenido: `supabase/migrations/20260830000000_lead_delivery_outbox_foundation_s5_005.sql` — fundación física del dominio de entrega de leads (`docs/f5-distribution-measurement-contract.md` §11: "Implement lead delivery (wiring of `restricted.lead_deliveries`, `outbox_events` processing, `lead_attribution`) per S0-016, disabled/synthetic adapters only"). Dos piezas: (1) `public.outbox_events` nueva (`docs/core-schema.md` §10.21), en `public` no en `restricted` — no aparece en la tabla de clasificación de datos personales de `core-schema.md` §14, consistente con `docs/lead-delivery-contract.md` §21 (el payload del outbox nunca duplica el lead/formulario completo). Vocabulario de estado propio de 5 valores genéricos de cola (`pending/processing/processed/failed/dead_letter`), decisión documentada en el header de la migración: ningún documento aprobado fija el vocabulario de `outbox_events` (a diferencia de `lead_deliveries`, que sí lo tiene en el contrato §27) — se trató como decisión de implementación razonada, no como ambigüedad de negocio que requiriera pregunta al usuario. Sin trigger de transición todavía (grafo no ratificado por ningún documento). (2) `restricted.lead_deliveries` (ya existía desde S1-010): se agregó `lead_deliveries_status_allowed` CHECK (7 estados oficiales del contrato §27) + trigger `lead_deliveries_validate_status_transition_trigger` con los 10 edges permitidos del contrato §28 (11 filas de la tabla del contrato menos "Creation → pending", que es el default de la columna, no un UPDATE real) — mismo patrón que `publications_validate_status_transition` (S5-002). `supabase/tests/database/lead_delivery_outbox_foundation_s5_005.test.sql` (34 aserciones pgTAP). Validado por el usuario: `npx supabase db reset && npx supabase test db` → `Files=46, Tests=1595, Result: PASS` (1561+34, exacto, sin regresiones).

Hallazgo operativo de esta iteración: el sandbox de Cowork usado esta sesión no tiene `sudo`/root, así que el patrón de validación local Postgres+pgTAP (Registro de Patrones) no estuvo disponible — a diferencia de sesiones anteriores bajo un entorno "Modo A" más privilegiado. Consecuencia real: el primer intento del archivo de test se entregó con `plan(33)` en vez de `plan(34)` (el asistente contó mal los edges del grafo del trigger — 10 edges reales, no 9 — sin poder atraparlo con una corrida local antes de entregar). Corregido en el segundo intento tras el `Bad plan` real reportado por el usuario. Guardado en memoria persistente de la sesión.

Deliberadamente fuera de alcance de esta iteración (documentado en el header de la migración, decisión del product owner sobre el alcance 2026-08-07): la RPC que crea `lead_delivery` + `outbox_event` atómicamente desde `create_submission` cuando un lead se clasifica `prefiltered`; el worker/adapter en segundo plano (ningún tipo de destino está seleccionado, contrato §59 lo bloquea "before delivery implementation"); la representación de historial de intentos append-preserving; `lead_attribution` (sigue sin columnas definidas en ningún documento aprobado); RLS por rol más allá de lo que S1-010 ya otorgó.

PR de cierre documental de la iteración 1 (este archivo + Registro de Patrones + `repomix-output.txt`) mergeada a `main` (PR #90 `docs/s5-005-iteracion-1-cierre`, merge commit `773fa91`, 3/3 checks, rama borrada, 2026-08-07).

**S5-005 — Iteración 2/N CERRADA e integrada a `main` con evidencia real (2026-08-07).** PR #91 `feat/f5-005-lead-delivery-creation-wiring` → `main`, mergeado (merge commit `d43177e`, 3/3 checks requeridos en verde, rama borrada). Contenido: `supabase/migrations/20260831000000_lead_delivery_creation_wiring_s5_005.sql` — reemplaza `public.create_submission` (misma firma, sin cambios en `route.ts`) agregando la creación atómica de `lead_delivery` + `outbox_event` cuando la clasificación es `prefiltered` (contrato de entrega §8/§10), con guardia contra duplicados: si el lead ya tiene una entrega `pending/processing/confirmed/retry_scheduled` al mismo destino, no crea una segunda (§11). Decisiones documentadas en el header de la migración: destino hardcodeado a un único `synthetic_sink`/`synthetic-destination-001` (mismos valores que `supabase/seed.sql`) — no existe tabla de configuración de destinos, y el contrato §59 bloquea el tipo de destino real "before delivery implementation"; la creación de entrega NO se gatea por `is_test` (siempre `true` hoy) porque ese flag es la bandera de entorno sintético del proyecto, no la clasificación de negocio `test` del contrato de entrega (que `create_submission` nunca produce); `delivery_version` no es columna física todavía, así que la clave de idempotencia embebe `v1` fijo; `correlation_id` del evento se genera nuevo dentro del payload (no se hiló el de la request real -- ningún worker lo consume todavía); `campaign_code` del payload se resuelve de `form_sessions.campaign_id → campaigns.code`, ya disponible en la consulta de sesión existente. `supabase/tests/database/lead_delivery_creation_wiring_s5_005.test.sql` (18 aserciones pgTAP). Validado por el usuario: `npx supabase db reset && npx supabase test db` → `Files=47, Tests=1613, Result: PASS` (1595+18, exacto, sin regresiones).

Deliberadamente fuera de alcance: el worker/adapter que reclama el outbox event (ningún tipo de destino real seleccionado todavía); re-versionar una entrega tras un estado terminal-negativo (no hay columna `delivery_version` para distinguirlo); `lead_attribution`.

Pendiente (no bloqueante, mismo segmento S5-005) resuelto: el product owner definió las dos decisiones que el contrato §59 bloqueaba "before worker implementation" (2026-08-07) — lease de procesamiento 5 minutos, lote de 10 eventos por invocación, un solo worker (sin concurrencia modelada todavía).

**S5-005 — Iteración 3/N CERRADA e integrada a `main` con evidencia real (2026-08-07).** PR #93 `feat/f5-005-lead-delivery-worker-synthetic-adapter` → `main`, mergeado (merge commit `8d2436d`, 3/3 checks requeridos en verde, rama borrada). Contenido: `supabase/migrations/20260901000000_lead_delivery_worker_synthetic_adapter_s5_005.sql` — worker claim (`public.claim_outbox_events`) + adaptador sintético/deshabilitado (`public.confirm_synthetic_delivery`), per contrato de entrega §30 (worker claim and lease). Agrega columnas de lease (`leased_by`, `lease_expires_at`) a `outbox_events` -- decisión documentada en el header de la migración: ningún documento aprobado las lista (`docs/core-schema.md` §10.21 sigue fijando diez columnas), pero el §30 las requiere para que "solo puede existir un lease activo por entrega" sea verificable, a diferencia de `correlation_id` (iteración 1) que sí cupo dentro de `payload`. `claim_outbox_events` reclama atómicamente un lote acotado (`pending` y vencido, o `processing` con lease expirado) vía `for update skip locked`, y en el mismo statement avanza el `lead_deliveries` asociado de `pending` a `processing`, reutilizando la columna genérica `version` (optimistic-concurrency counter de S1-010) como la "versión esperada" del §30 en vez de crear una columna `delivery_version` nueva (distinción ya fijada por el header de la iteración 2). `confirm_synthetic_delivery` es la única operación del adaptador deshabilitado: verifica que el llamador sostenga el lease vigente, confirma la entrega (solo acepta `destination_type = 'synthetic_sink'`) y marca el outbox event `processed` -- sin ninguna llamada externa real. Deliberadamente fuera de alcance (mismo razonamiento que iteraciones 1-2): retry/dead-letter/historial de intentos append-preserving (§31-37) -- un adaptador que nunca hace una llamada externa real no puede producir un resultado retryable/ambiguo, y el §32 ya implica que ni `lead_deliveries.attempt_count` ni `outbox_events.attempt_count` deben incrementarse en esta iteración; un adaptador real (email/webhook/inbox) -- sigue sin tipo de destino real seleccionado (§59). `supabase/tests/database/lead_delivery_worker_synthetic_adapter_s5_005.test.sql` (43 aserciones pgTAP, contadas con `grep` antes de fijar `plan(43)`, mismo patrón de Registro de Patrones que evitó el error de plan de la iteración 1). Validado por el usuario: `npx supabase db reset && npx supabase test db` → `Files=48, Tests=1656, Result: PASS` (1613+43, exacto, sin regresiones).

Hallazgo operativo de esta iteración: el primer intento del PR de código (commit `c5cdb43`) falló el check "Secret scanning" -- `gitleaks`, regla `generic-api-key`, línea 426 del archivo de test, disparada por la forma literal `idempotency_key = 'valor'` en tres fixtures nuevas de guardia (iteraciones 1-2 solo comparaban `idempotency_key` columna-contra-columna, nunca un string literal en el código fuente). No se tocó `.gitleaks.toml`: se reescribieron las tres búsquedas para filtrar por `aggregate_id` (columna estructural, no un valor con forma de secreto) en vez de por el literal de `idempotency_key`, mismo dato, sin la forma que gitleaks marca. Corregido con `git commit --amend` + `git push --force-with-lease` (mismo commit, sin dejar el string marcado en el historial); segundo intento (`eebdc71`) con 3/3 checks en verde. Guardado como patrón nuevo en Registro de Patrones. PR de cierre documental de esta iteración (este archivo + Registro de Patrones + `repomix-output.txt`) mergeada a `main` (PR #94 `docs/s5-005-iteracion-3-cierre`, merge commit `c157e05`, 3/3 checks, rama borrada, 2026-08-07).

**S5-005 — CERRADO como segmento (2026-08-07).** Decisión del product owner (confirmada explícitamente): cerrar S5-005 sin construir una iteración 4. `docs/f5-distribution-measurement-contract.md` §11 fija la responsabilidad exacta de `S5-005` como "Implement lead delivery (wiring of `restricted.lead_deliveries`, `outbox_events` processing, `lead_attribution`) per S0-016, disabled/synthetic adapters only" — las tres iteraciones cerradas (2026-08-07) cubren esa responsabilidad con evidencia real: fundación del outbox + estado de `lead_deliveries` (iter. 1), creación atómica `lead_delivery`+`outbox_event` desde `create_submission` (iter. 2), worker claim + adaptador sintético/deshabilitado (iter. 3) — exactamente "disabled/synthetic adapters only"; ningún tipo de destino real fue construido ni autorizado (Gate G4, contrato §59 sigue bloqueando "before delivery implementation"). Los dos pendientes discutidos no son responsabilidad de S5-005 por el propio contrato:
- Retry/dead-letter (§31-37) — el propio contrato de entrega condiciona esa lógica a que exista un adaptador real que pueda fallar de forma retryable/ambigua; el adaptador sintético nunca falla, así que no hay comportamiento real que probar todavía.
- RLS de estado para roles humanos sobre `lead_deliveries`/`outbox_events` (`docs/access-control-matrix.md` §16, "Human roles: status only when required") — pertenece formalmente a `S5-006` ("Implement F5 RLS and storage/API authorization for the full domain"), el siguiente segmento en el orden del contrato §11, no a S5-005.

`lead_attribution` (mencionado en la propia responsabilidad de S5-005 por el contrato §11) sigue sin columnas definidas en ningún documento aprobado — mismo pendiente no bloqueante arrastrado desde la iteración 1, se traslada intacto al segmento que lo resuelva.

Ambos quedan como trabajo pendiente no bloqueante para el resto de F5 (ningún segmento posterior depende de ellos para avanzar) ni para Gate G5 — mismo tratamiento que S5-004 le dio a rate limiting/honeypot/origin allowlisting. Próximo segmento: `S5-006` (RLS y autorización de API/storage para todo el dominio F5, per `docs/access-control-matrix.md` §§12-15, preservando calificadores no soportados fail-closed).

**S5-006 — Iteración 1/N (código listo en disco, sin validar con evidencia real todavía, 2026-08-07).** `supabase/migrations/20260902000000_publications_tracking_links_role_based_rls_s5_006.sql` — RLS por rol para `public.publications` y `public.tracking_links` (`docs/access-control-matrix.md` §12, "Publication matrix"), cerrando la postura "Foundation, not yet connected" que ambas tablas cargaban desde S5-002/S5-003 iteración 1. Alcance: únicamente las celdas SIN calificador de §12 (publisher `L R C U T`, approver `L R A`/`R`, campaign_manager `L R`, results_analyst `L R`) — decisión que replica al pie de la letra el precedente ya sentado por S3-007 ("Core-RLS scope, mirroring the S2-009 precedent exactly... every qualified cell is explicitly NOT implemented here"). La celda calificada de commercial_owner ("Related R T pause" en publications, "Related R" en tracking_links) queda deliberadamente sin construir — ningún helper ni columna física define "related" para ese rol contra una publicación o tracking link, mismo tipo de vacío que S3-007 dejó documentado para Gate G3, aquí documentado para Gate G5. La fila "Public asset copies" de §12 tampoco se tocó: no existe tabla física para ese concepto en `docs/core-schema.md`. `T` (transición explícita de publisher) y `A` (approve/reject de approver) se plegaron a un GRANT UPDATE liso, mismo pliegue que ya usó S4-008 — no existe todavía un servicio de transición controlado separado para `publications` (el propio contrato §4.2 lo sigue difiriendo), así que el único mecanismo físico es un UPDATE directo, ya gateado por completo por `publications_validate_status_transition_trigger` (S5-002 iteración 1) sin importar el rol. Reutiliza `public.has_active_role(text)` (S1-004), el mismo helper de S4-008, el precedente de cierre de RLS más reciente del repositorio. `supabase/tests/database/publications_tracking_links_role_based_rls_s5_006.test.sql` (12 aserciones, estructurales únicamente — `has_table_privilege` + conteo de `pg_policies` — mismo corte que `production_qa_role_based_rls_s4_008.test.sql`; la prueba de comportamiento por fila con sesiones de rol simuladas queda para `S5-009`, la suite transversal de autorización de F5, mismo split que S3-007/S3-008 y S4-008/S4-010 ya establecieron). Dos aserciones obsoletas por diseño se actualizaron en los archivos de test de la foundation (`publications_lifecycle_s5_002.test.sql`, `tracking_links_foundation_s5_003.test.sql`): "authenticated no tiene privilegio" pasó a "authenticated sí tiene privilegio (S5-006 lo agregó)", mismo patrón ya registrado en Registro de Patrones.

Fuera de alcance de esta iteración, documentado en el header de la migración (decisión de arquitectura, no un olvido): el resto de la matriz F5 — `form_sessions`/`restricted.form_submissions`/`restricted.leads`/`restricted.lead_consents`/`restricted.lead_attribution`/`restricted.lead_deliveries`/`public.outbox_events` (§13-14, §16) — no puede recibir RLS+grant clásico vía Data API porque `restricted` no está en `supabase/config.toml`'s `schemas = ["public", "graphql_public"]` (mismo hecho ya documentado en S5-004): un grant a `authenticated` ahí sería un no-op silencioso a través de PostgREST, o peor, exigiría exponer `restricted` por la Data API contradiciendo directamente el enfoque que S1-010 ya fijó ("Restricted schema not exposed through the Data API", §14.3). La autorización de esa matriz PII solo puede vivir en las rutas privadas que construya `S5-008` (service_role + autorización a nivel de ruta + shaping de respuesta), no en una migración de RLS de tabla. `metric_definitions`/`metric_observations`/`campaign_reports` (§15) son alcance de `S5-007`, todavía sin construir.

Validado por el usuario: `npx supabase db reset && npx supabase test db` → `Files=49, Tests=1668, Result: PASS` (1656+12, exacto, sin regresiones). PR de código #97 (`feat/f5-006-publications-tracking-links-role-rls`, merge commit `26da122`, 3/3 checks) + PR de cierre documental #98 (`docs/s5-006-iteracion-1-cierre`, merge commit `c0cd7c3`), ambos en `main`, ramas borradas.

**S5-006 — Iteración 2/N (código listo en disco, sin validar con evidencia real todavía, 2026-08-07).** `supabase/migrations/20260903000000_publications_tracking_links_commercial_owner_related_rls_s5_006.sql` — resuelve la celda calificada que la iteración 1 dejó deliberadamente afuera: commercial_owner "Related R T pause" en `publications`, "Related R" en `tracking_links`. "Related" reutiliza, sin reinventar, la definición que S3-006 ya fijó para la misma palabra sobre la familia evidence/claims: `campaigns.owner_profile_id = current_profile_id()`. A diferencia de S3-006 (que necesitó un helper SECURITY DEFINER porque `campaigns` todavía era "Foundation, not yet connected" en ese momento), `campaigns` ya está RLS-cerrada desde S3-007, así que un EXISTS directo contra `public.campaigns` alcanza sin helper nuevo. `publications`/`tracking_links` tienen columna `campaign_id` propia, sin necesidad de atravesar tablas intermedias como sí tuvo que hacer S3-006 con `campaign_evidence`. "T pause" se implementó como una política de UPDATE separada cuyo `WITH CHECK` exige `publications.status = 'paused'` en la fila resultante — la validez de la arista en sí (`scheduled -> paused` / `published -> paused`) la sigue resolviendo únicamente `publications_validate_status_transition_trigger` (S5-002 iteración 1), sin duplicar esa lógica en RLS. `supabase/tests/database/publications_tracking_links_commercial_owner_related_rls_s5_006.test.sql` (11 aserciones) — a diferencia del test estructural de la iteración 1, este es comportamental con sesiones reales por rol (`set local role authenticated` + `request.jwt.claim.sub`), mismo método que `evidence_claims_family_rls_extension_s3_006.test.sql` ya probó para el mismo calificador: dos commercial_owner con campañas propias, cada uno ve solo su publicación/tracking_link, cada uno puede pausar solo la propia, ningún otro estado, y ninguno puede tocar la fila del otro (0 filas por USING) ni insertar (sin política de insert para este rol).

Con esto, `docs/access-control-matrix.md` §12 (matriz de `publications`) queda completo: las cuatro celdas sin calificador (iteración 1) más la única celda calificada (iteración 2, commercial_owner). `S5-006` queda cerrado para las tablas `public` que existen hoy. El resto del alcance de §11 del contrato ("RLS y autorización de API/storage para todo el dominio F5, per §§12-15") no es trabajo pendiente de S5-006 sino de otros segmentos ya identificados por su propio número: §14 (matriz PII de `restricted.*`) es `S5-008` (rutas privadas, porque `restricted` no está expuesto por la Data API — ver el header de la migración de iteración 1); §15 (`metric_definitions`/`metric_observations`/`campaign_reports`) es `S5-007`, tablas que todavía no existen. Pendiente inmediato: validar con evidencia real del usuario (`npx supabase db reset && npx supabase test db`) — mismo hallazgo operativo de sandbox sin Postgres+pgTAP local ya registrado en S5-005/iteración 1 de S5-006.

**S5-007 — Iteración 1/N CERRADA e integrada a `main` con evidencia real (2026-08-08).** PR #101 `feature/s5-007-metric-foundation-iter1` → `main`, mergeado (merge commit `355202c`, 3/3 checks requeridos en verde — CI/Migration and schema/RLS checks, CI/Quality and Cloudflare build, CI/Secret scanning —, rama borrada). Contenido: `supabase/migrations/20260904000000_metric_definitions_observations_foundation_s5_007.sql` — fundación física de `public.metric_definitions` (`docs/core-schema.md` §6.7; contrato §7.1: nombre canónico versionado, unidad y fórmula; `(name, version)` único; `status` `active`/`deprecated` para poder deprecar en vez de borrar una definición referenciada) y `public.metric_observations` (contrato §7.2: valor de métrica por campaña, publicación opcional, período y fuente; `metric_definition_id` `on delete restrict`; `publication_id` nullable). Postura "Foundation, not yet connected": RLS habilitado, sin grants a `authenticated`; `metric_definitions` otorga `select/insert/update` a `service_role` (foundation mutable, como `publications`/`tracking_links`); `metric_observations` otorga solo `select/insert` (append-preserving por contrato — "a corrected value creates a new observation rather than overwriting a prior one" — mismo corte que `generation_attempts`/`approvals`). `source` de `metric_observations` es texto normalizado por defecto `synthetic`, sin CHECK cerrado a ese único valor — mismo razonamiento ya documentado por `publications.platform` en S5-002 iteración 1 (garantía estructural por ausencia de I/O externo en esta migración, no un allowlist de base de datos; la superficie real de riesgo es la API privada de S5-008). `formula` queda como texto libre único, no descompuesto en columnas `numerator`/`denominator` pese a que `docs/access-control-matrix.md` §15.1 menciona ambos — el contrato §7.1 solo exige "unit and formula" y deja las columnas exactas como decisión abierta; flageado en el header de la migración, no resuelto. `supabase/tests/database/metric_definitions_observations_foundation_s5_007.test.sql` (25 aserciones pgTAP, estructurales + defaults + CHECK/UNIQUE/FK). Validado por el usuario: `npx supabase db reset && npx supabase test db` → `Files=51, Tests=1704, Result: PASS` (1679+25, exacto, sin regresiones).

Deliberadamente fuera de alcance de esta iteración (documentado en el header de la migración, misma partición S5-002 iteración 1 / S5-006 iteración 1 ya usada para `publications`): RLS por rol para ambas tablas (`docs/access-control-matrix.md` §15) — iteración separada de este mismo segmento; el trigger que bloquee la mutación de `name`/`version`/`unit`/`formula` de una `metric_definitions` ya referenciada por una observación (contrato §7.1) — el FK `on delete restrict` ya bloquea el borrado, la mutación queda pendiente; `campaign_reports` (§7.3, P2) — explícitamente diferido por el propio contrato.

Pendiente (no bloqueante, mismo segmento S5-007): decidir con el product owner si `formula` se descompone en columnas estructuradas antes o después de construir el RLS por rol; PR de cierre documental de esta iteración (este archivo + Registro de Patrones + `repomix-output.txt`, arrastrado de sesiones previas).

**S5-007 — Iteración 2/N CERRADA e integrada a `main` con evidencia real (2026-08-08).** PR #103 `feature/s5-007-metric-rls-iter2` → `main`, mergeado (merge commit `48b8250`, 3/3 checks requeridos en verde, rama borrada). Contenido: `supabase/migrations/20260905000000_metric_definitions_observations_role_based_rls_s5_007.sql` — RLS por rol para `metric_definitions`/`metric_observations` (`docs/access-control-matrix.md` §15, "Measurement and learning matrix"), cerrando la postura "Foundation, not yet connected" que ambas tablas cargaban desde la iteración 1. Alcance: únicamente las celdas SIN calificador de §15, mismo corte "Core-RLS" que S5-006 iteración 1 (`metric_definitions`: results_analyst `L R C U M` → select/insert/update, `M` plegado a `U` sin mecanismo físico separado; campaign_manager/commercial_owner/investment_analyst `L R`/`R` bare → select. `metric_observations`: results_analyst `L R C U` controlled → select/insert únicamente, sin `update` — la palabra "controlled" se leyó contra la regla append-preserving del contrato §7.2, misma lectura que la iteración 1 ya le dio al mismo término al otorgarle a `service_role` solo `select, insert`; campaign_manager/commercial_owner `L R` → select). Dos celdas calificadas quedan deliberadamente sin construir, documentadas en el header de la migración: "Other roles: Approved R" en `metric_definitions` — esta tabla nunca tiene un estado `approved` (solo `active`/`deprecated`), a diferencia de `claims`/`content_versions` (S2-009) donde "Approved" sí reutiliza `state_transition_subjects.current_state='approved'` real; inventar esa correspondencia aquí violaría el §8 del contrato ("Any authorization qualifier that is not backed by an enforceable physical relationship... must fail closed"). Las celdas "Related R" (investment_analyst) y "Related aggregate R" (otros roles) en `metric_observations` — el propio §8 del contrato ya fija que el calificador "Related" de F2/F3 sigue abierto y bloqueante por la condición 2 de Gate G4, y que "F5 neither closes nor worsens any of them"; a diferencia del "Related" de commercial_owner en `publications`/`tracking_links` (S5-006 iteración 2, resuelto vía `campaigns.owner_profile_id`), no existe ninguna definición física reutilizable para el "Related" de investment_analyst contra una observación de métrica. `supabase/tests/database/metric_definitions_observations_role_based_rls_s5_007.test.sql` (12 aserciones, estructurales únicamente — mismo corte que `publications_tracking_links_role_based_rls_s5_006.test.sql`; la prueba de comportamiento por fila con sesiones de rol simuladas queda para `S5-009`).

Hallazgo real de esta iteración: el primer intento rompió 2 aserciones ya existentes en `metric_definitions_observations_foundation_s5_007.test.sql` (iteración 1) — "authenticated no tiene privilegio" sobre ambas tablas, obsoletas por diseño en cuanto esta migración conectó `authenticated` vía RLS, mismo patrón exacto ya registrado en Registro de Patrones para `publications_lifecycle_s5_002.test.sql`/`tracking_links_foundation_s5_003.test.sql` cuando S5-006 iteración 1 las conectó. Corregidas en el mismo PR. Validado por el usuario: `npx supabase db reset && npx supabase test db` → primer intento `Files=52, Tests=1716, Result: FAIL` (2 fallas, tests 4 y 9 del archivo de fundación); segundo intento tras la corrección → `Files=52, Tests=1716, Result: PASS` (1704+12, exacto, sin regresiones reales).

**S5-007 — CERRADO como segmento (2026-08-08).** `docs/f5-distribution-measurement-contract.md` §11 fija la responsabilidad exacta de `S5-007` como "Implement `metric_definitions` and `metric_observations` per §7" — ambas entidades P1 están construidas con evidencia real (iteración 1: fundación física; iteración 2: RLS por rol de las celdas sin calificador de §15), mismo patrón de cierre por texto normativo ya usado para S5-004/S5-005/S5-006. `campaign_reports` (§7.3) sigue explícitamente P2/diferido por el propio contrato — no es responsabilidad de S5-007. Pendientes no bloqueantes, arrastrados como gaps documentados para Gate G5, mismo tratamiento que S5-006 le dio a la matriz PII de `restricted.*`:
- Las dos celdas calificadas de §15 ("Approved R" en `metric_definitions`, "Related R"/"Related aggregate R" en `metric_observations`) — no resueltas, contrato §8 fail-closed.
- El trigger que bloquee la mutación de `name`/`version`/`unit`/`formula` de una `metric_definitions` ya referenciada por una observación (contrato §7.1) — el FK `on delete restrict` ya bloquea el borrado; la mutación queda sin gate propio.
- Decidir si `metric_definitions.formula` se descompone en columnas `numerator`/`denominator` estructuradas (`docs/access-control-matrix.md` §15.1) — sigue como texto libre único.
- Prueba de comportamiento por fila con sesiones de rol simuladas para ambas tablas — `S5-009`.

Próximo segmento sin decidir todavía: `S5-008` (la API privada de F5 — rutas de publicación, captura, entrega y medición) es el siguiente en el orden del contrato §11, y es también donde debe vivir la autorización de la matriz PII de `restricted.*` que S5-006 ya dejó documentada como fuera de alcance de RLS de tabla.

**S5-008 — Iteración 1/N CERRADA e integrada a `main` con evidencia real (2026-08-08).** PR #105 `feature/s5-008-publications-tracking-links-routes` → `main`, mergeado (merge commit `8c8a40f`, 3/3 checks requeridos en verde, rama borrada). Primera iteración de la API privada de F5 (`docs/f5-distribution-measurement-contract.md` §11: "Implement the private F5 API (publication, capture, delivery, measurement routes)"). Investigación previa relevante: el catálogo de acciones de autorización (`src/lib/auth/authorization.ts`) ya tenía reservadas `publication.read`/`publication.write`/`publication.approve` desde el diseño original de S1-003 (antes de que F5 existiera), nunca conectadas a ninguna ruta hasta esta iteración. Contenido: `src/app/api/v1/publications/route.ts` y `src/app/api/v1/tracking-links/route.ts` — GET/POST vía el factory genérico `createListHandler`/`createCreateHandler` (S2-009), mismo patrón `evidence_items`/`sources`: sin RPC nueva, camino `userClient` + RLS puro (la RLS ya la cerraron S5-006/S5-007). `src/lib/auth/authorization.ts` — conecta las tres acciones `publication.*` ya reservadas, y agrega el par nuevo `tracking_link.read`/`tracking_link.write` (acción propia por tabla, misma convención que cada sub-tabla F4/F5 ya documentada). Decisión de diseño: `status` (publications) y `token`/`status` (tracking_links) quedan deliberadamente FUERA del conjunto de campos aceptados por la ruta — son columnas con default/trigger propios (`publications_validate_status_transition_trigger`, `generate_tracking_token()`, `tracking_links_supersede_prior_active_trigger`); aceptarlas desde el body abriría un bypass. `tests/api/publications-route-authorization.test.ts` y `tests/api/tracking-links-route-authorization.test.ts` (5 tests cada uno, mismo patrón que `private-route-authorization.test.ts`, S2-009).

Hallazgo real de esta iteración: el primer intento de ambos archivos de test tenía un `fakeUserClient()` que nunca aceptaba `user: null`, a diferencia del `fakeUserClient` de `private-route-authorization.test.ts` — el test "unauthenticated" pasaba de largo el chequeo 401 y llegaba a `createServiceRoleClient()` sin mockear, devolviendo 503 en vez de 401 (2 fallas reales reportadas por el usuario). Corregido agregando el parámetro `user` nullable a ambos `fakeUserClient`, mismo patrón que el archivo de referencia. Validado por el usuario: `npm test` → primer intento `Files=51, Tests=415` con 2 fallas (`expected 503 to be 401` en ambos archivos nuevos); segundo intento → `Files=51, Tests=415, all passed` (405+10, exacto, sin regresiones). `tsc --noEmit`/`eslint .` limpios (verificados en el sandbox del asistente antes de cada entrega).

Deliberadamente fuera de alcance de esta iteración (documentado en el header de ambas rutas): una ruta PATCH/transition dedicada para `publications` (el servicio de transición controlado con RPC sigue siendo un gap no bloqueante heredado del cierre de S5-002); `metric_definitions`/`metric_observations` (ya tienen RLS propia desde S5-007 pero su propia ruta privada es una iteración separada); la matriz PII de `restricted.*` (`leads`/`lead_deliveries`/etc., `docs/access-control-matrix.md` §14) — necesita diseño de enmascarado/shaping de campos, alcance mayor que esta iteración.

Pendiente (no bloqueante, mismo segmento S5-008): decidir el orden de las próximas iteraciones (rutas de `metric_definitions`/`metric_observations`, o la matriz PII de leads) y PR de cierre documental de esta iteración (este archivo).

## Bloque C — Contrato / negocio

| Pieza | Estado | Referencia |
|---|---|---|
| Patrón "Foundation, not yet connected" | RESUELTO | ver Registro de Patrones |
| F6 (Aprendizaje) es track paralelo ya cerrado | RESUELTO | ver Registro de Patrones — no tratar archivos/migraciones F6 como fuera de secuencia |
| Alcance real de S4-009 (no acotado a un dominio) | RESUELTO | `docs/f4-production-qa-contract.md` §21 — corregido 2026-08-04, ver histórico en memoria de sesión previa |

---

## Nota de migración (2026-08-05)

Este archivo reemplaza el contenido que en la Metodología 3.0 vivía únicamente como texto dentro de los testigos y del project memory persistente del asistente. Bajo 4.0 / Modo A, es un archivo físico en la raíz del repositorio que el asistente lee y actualiza directamente. El project memory del asistente conserva un puntero a este archivo pero ya no es la fuente de verdad del Índice.
