## Summary

- S5-009: implements the transversal F5 cross-surface authorization test suite, per `docs/f5-distribution-measurement-contract.md` §11, scoped concretely in `docs/authorization-test-map.md` §8 (new section this PR adds).
- New file `supabase/tests/database/cross_surface_authorization_test_suite_s5_009.test.sql` (94 assertions, landed in 3 slices within the same file, same discipline `cross_surface_authorization_test_suite_s4_010.test.sql` established):
  - **Slice 1** — `publications`/`tracking_links` (§12), unqualified cells: publisher, approver, campaign_manager, results_analyst. Full SELECT/INSERT/UPDATE/DELETE boundary proof with real role-simulated sessions, not grant/policy-existence structural checks. `commercial_owner`'s "Related" qualifier is out of scope here — already proven behaviorally in S5-006 iteration 2.
  - **Slice 2** — `public.form_sessions` (§14), the one remaining behavioral gap: administrator's direct-table `Restricted L R`. Reuses slice 1's own fixtured roles rather than re-fixturing them.
  - **Slice 3** — `metric_definitions`/`metric_observations` (§15), unqualified cells: results_analyst, campaign_manager, commercial_owner, investment_analyst. Proves the investment_analyst asymmetry between the two tables (bare `R` on `metric_definitions`, but zero access on `metric_observations` since its "Related R" cell is deliberately unimplemented).
- New corrective migration `supabase/migrations/20260913000000_generate_tracking_token_authorization_s5_009.sql` — fixes a **real, production-breaking regression** this item's own behavioral testing found on its first real run: `generate_tracking_token()` (S5-003 iteration 1) was still locked to `service_role`-only EXECUTE, so every authenticated `tracking_links` INSERT that omits `token` — the only shape the real route (`POST /api/v1/tracking-links`) ever sends — has been failing with "permission denied for function generate_tracking_token" since S5-006 iteration 1 opened direct publisher INSERT on that table. Same bug class S4-001 (`generate_claim_code`) and S3-008 (the four `generate_*_code` functions) already found for the identical DEFAULT-expression-runs-as-caller reason.
- `supabase/tests/database/tracking_links_foundation_s5_003.test.sql` updated in place: one structural assertion ("Authenticated cannot execute generate_tracking_token") was obsoleted by the fix above, the same way S5-006 iteration 1 had already obsoleted that file's SELECT-privilege assertion without the EXECUTE one being updated to match.
- `docs/authorization-test-map.md` extended (not overwritten) with new §8, documenting S5-009's concrete scope, the regression found and fixed, and two test-authoring corrections made during development (both self-contained, no schema impact):
  - Four/nine UPDATE-denial assertions were initially written as `throws_ok('42501', ...)`, copying the INSERT-denial shape. UPDATE denial-by-missing-policy is not an error in Postgres RLS — the USING clause filters the candidate row set to empty and the statement completes having changed nothing. Rewritten to `is_empty(... returning id)`, mirroring `publications_tracking_links_commercial_owner_related_rls_s5_006.test.sql`'s own precedent.
  - Slice 2's fixture INSERT initially inherited slice 1's last `set local role authenticated` session instead of running unprivileged — fixed by adding `reset role;` before each new slice's own fixture.

## Test plan

- [x] `npx supabase db reset` then `npx supabase test db` — full suite green after both the schema regression and the two test-authoring corrections above: `Files=60, Tests=1957, Result: PASS`. No other file regressed.
- [x] All three planned slices from `docs/authorization-test-map.md` §8.1 land in this PR — no table or role cell named in that plan remains untested.
- [x] Tests use no production identities, secrets or lead data — all fixtures use `*.test` synthetic emails and fixed synthetic UUIDs.
- [x] A regression is caught and fixed by this item's own behavioral testing, not merely by inspection — `generate_tracking_token()`'s missing `authenticated` EXECUTE grant, live and production-breaking since S5-006 iteration 1.
